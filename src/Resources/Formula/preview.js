window.renderFormula = function(text) { const target = document.getElementById('formula'); try { target.className=''; katex.render(text, target, {displayMode:true,throwOnError:true,trust:false,maxExpand:1000,maxSize:30,output:'htmlAndMathml'}); return ''; } catch (error) { target.textContent = String(error.message); target.className='katex-error'; return String(error.message); } }; window.formulaBounds = function() { const r=document.getElementById('formula').getBoundingClientRect(); return {width:r.width,height:r.height}; }; window.formulaMathML=function(){return document.querySelector('.katex-mathml')?.innerHTML || '';};