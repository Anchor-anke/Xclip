# 运行时许可来源

这些文件复制自微软 NuGet 包 `Microsoft.NETCore.App.Runtime.win-x64` 和 `Microsoft.WindowsDesktop.App.Runtime.win-x64` 的 10.0.12 版本，随当前 .NET 10 SDK 构建的 Windows 预览包分发。

更新运行时版本时应核对包内许可与第三方声明，并同步此目录。项目本身的许可见根目录 LICENSE 和 NOTICE.md。
